# Инструкция по деплою на GitHub Pages

## Что было сделано

✅ Настроен статический экспорт Next.js для GitHub Pages
✅ Создан GitHub Actions workflow для автоматического деплоя
✅ Исправлены серверные функции для работы со статическим экспортом
✅ Обновлена форма контактов для работы без серверного API

## Шаги для деплоя

### 1. Подготовка репозитория

```bash
# Закоммить все изменения
git add .
git commit -m "Настроен деплой на GitHub Pages"

# Если репозиторий еще не создан на GitHub
git remote add origin https://github.com/USERNAME/REPOSITORY_NAME.git
git push -u origin main
```

### 2. Настройка GitHub Pages

1. Перейдите в настройки репозитория: **Settings → Pages**
2. В разделе **Source** выберите **GitHub Actions**
3. Сохраните настройки

### 3. Запуск деплоя

После настройки GitHub Pages:
- При каждом пуше в ветку `main` автоматически запустится деплой
- Или запустите вручную: **Actions → Deploy to GitHub Pages → Run workflow**

### 4. Опциональные настройки

Если нужен кастомный домен или basePath, добавьте секреты в **Settings → Secrets and variables → Actions**:

- `NEXT_PUBLIC_BASE_URL` - полный URL сайта (например, `https://yourdomain.com`)
- `NEXT_PUBLIC_BASE_PATH` - базовый путь (если сайт в подпапке, например `/razmetka2`)
- `NEXT_PUBLIC_API_ENDPOINT` - внешний API endpoint для формы (например, `https://api.example.com/lead`)

## Важно знать

⚠️ **API Routes не работают**
- Статический экспорт не поддерживает серверные API routes
- Форма контактов использует `mailto:` как fallback
- Для полноценной работы формы используйте внешний API (Formspree, EmailJS и т.д.)

📝 **URL сайта**
- Если репозиторий называется `username.github.io` → `https://username.github.io`
- Если репозиторий называется `razmetka2` → `https://username.github.io/razmetka2`
- BasePath настраивается автоматически в workflow

🔧 **Локальная проверка**
```bash
npm run build
# Проверьте папку out/ - это статический экспорт
```

## Troubleshooting

**Деплой не запускается:**
- Проверьте, что в Settings → Pages выбран источник "GitHub Actions"
- Проверьте, что workflow файл находится в `.github/workflows/deploy.yml`

**Сайт открывается, но ресурсы не загружаются:**
- Проверьте, что `NEXT_PUBLIC_BASE_PATH` настроен правильно
- Убедитесь, что basePath в workflow совпадает с реальным путем

**Форма не отправляется:**
- Это нормально для статического экспорта
- Настройте `NEXT_PUBLIC_API_ENDPOINT` для использования внешнего API
- Или используйте `mailto:` fallback (уже настроен)
